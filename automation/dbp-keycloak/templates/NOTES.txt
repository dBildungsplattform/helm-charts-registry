{{ if (or (or .Values.auth.existingSecret .Values.extraConfig.external) .Values.extraSecret.external) }}
You are using existing secrets, which will not automatically restart the deployment.
{{ end }}
